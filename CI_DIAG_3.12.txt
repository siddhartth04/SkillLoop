PYTHON=3.12 EXITCODE=1
=== LAST 60 LINES RAW ===
______________________________ test_key_rotation _______________________________

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f4a2e49aab0>

    def test_key_rotation(monkeypatch):
        """On a rate-limit error the client switches to the next key in OPENAI_API_KEYS before sleeping."""
        openai = pytest.importorskip("openai", reason="provider extra not installed; core is stdlib-only")
        monkeypatch.setenv("OPENAI_API_KEYS", "k1,k2,k3")
        monkeypatch.delenv("OPENAI_API_KEY", raising=False)
        llm = LLM(provider="openai")
        assert llm._keys == ["k1", "k2", "k3"]
        hits = []
        def fake_create(**kw):
            hits.append(llm.client.api_key)
            if llm.client.api_key in ("k1", "k2"):
                e = openai.RateLimitError("429 Rate limit reached. Please try again in 500ms",
                                          response=type("R", (), {"status_code": 429, "headers": {}, "request": None})(), body=None)
                raise e
            return "ok"
        monkeypatch.setattr(llm, "_complete", lambda *a, **k: fake_create())
        assert llm.complete("s", "u") == "ok"
>       assert hits == ["k1", "k2", "k3"] and llm.active_key_index == 2
E       AssertionError: assert (['k2', 'k3'] == ['k1', 'k2', 'k3']
E         
E         At index 0 diff: 'k2' != 'k1'
E         Right contains one more item: 'k3'
E         
E         Full diff:
E           [
E         -     'k1',
E               'k2',
E               'k3',
E           ])

tests/test_loop.py:466: AssertionError
=============================== warnings summary ===============================
tests/test_loop.py::test_full_loop
tests/test_loop.py::test_principles
tests/test_loop.py::test_per_bullet_feedback_and_pruning
tests/test_loop.py::test_concurrent_writes_thread_local_connections
tests/test_loop.py::test_verified_by_does_not_flip_booleans
tests/test_purpose.py::test_mistake_is_learned_then_not_repeated_under_new_phrasings
tests/test_purpose.py::test_repeat_mistake_rate_drops
  /home/runner/work/SkillLoop/SkillLoop/skillloop/core.py:611: RuntimeWarning: SkillLoop: 3+ traces are queued but nothing is learning from them. Call loop.process() after tasks, use SkillLoop(auto_process=True), or set SKILLLOOP_AUTO_PROCESS=1.
    return self._learn_body(t, san)

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED tests/test_loop.py::test_key_rotation - AssertionError: assert (['k2', 'k3'] == ['k1', 'k2', 'k3']
  
  At index 0 diff: 'k2' != 'k1'
  Right contains one more item: 'k3'
  
  Full diff:
    [
  -     'k1',
        'k2',
        'k3',
    ])
1 failed, 101 passed, 7 warnings in 16.60s
