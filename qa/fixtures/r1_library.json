[
  {"name": "yaml-single-key-edit",
   "description": "Change one value in a YAML config file when the same leaf key also appears under other parents.",
   "applies_when": ["yaml config", "nested yaml key", "edit one key", "yaml value"],
   "symptoms": ["sibling key also changed", "sed edited two lines"],
   "not_for": ["json", "ini", "toml", "creating a config"],
   "queries": ["set replicas under service to 4", "change the timeout in the yaml", "update one key in conf.yaml",
               "edit a nested value in the yaml file", "change replicas but not limits", "bump the port in the yaml",
               "my sed edited the wrong yaml line", "set a single field in the yaml"]},

  {"name": "yaml-merge-two-files",
   "description": "Merge two YAML documents into one, deciding how conflicting keys and lists combine.",
   "applies_when": ["merge yaml", "combine yaml files", "yaml overlay", "yaml deep merge"],
   "symptoms": ["lists replaced instead of appended", "overlay lost keys"],
   "not_for": ["editing one key", "json", "validating a schema"],
   "queries": ["merge two yaml files", "combine base and overlay yaml", "deep merge my yaml configs",
               "overlay a yaml on top of another", "join two config files into one", "how do i merge yaml lists",
               "flatten two yaml documents together", "apply a yaml patch file"]},

  {"name": "pip-externally-managed-env",
   "description": "Install a Python package when pip refuses with an externally-managed-environment error.",
   "applies_when": ["pip install fails", "externally managed environment", "system python"],
   "symptoms": ["externally-managed-environment", "error: This environment is externally managed"],
   "not_for": ["import errors after a successful install", "conda", "npm"],
   "queries": ["pip install is blocked", "externally managed environment error", "cant install a package with pip",
               "pip says the environment is externally managed", "how do i install this python package",
               "pip refuses to install anything", "package install fails on ubuntu", "pip wont let me install"]},

  {"name": "pip-import-name-mismatch",
   "description": "Resolve a ModuleNotFoundError that happens after a package installed successfully, because the import name differs from the distribution name.",
   "applies_when": ["module not found after install", "import name differs", "wrong import name"],
   "symptoms": ["ModuleNotFoundError", "No module named"],
   "not_for": ["install failures", "virtualenv activation", "syntax errors"],
   "queries": ["installed it but cant import it", "modulenotfounderror after pip install",
               "no module named even though it installed", "pip said success but python cant find it",
               "why cant python see the package i installed", "import fails but the package is there",
               "the library installed fine but the import breaks", "wrong module name on import"]},

  {"name": "git-rebase-conflict-resolve",
   "description": "Resolve conflicts during an interactive rebase without losing commits.",
   "applies_when": ["rebase conflict", "interactive rebase", "replay commits"],
   "symptoms": ["CONFLICT (content)", "rebase stopped", "detached HEAD during rebase"],
   "not_for": ["merge conflicts on a merge", "force pushing", "reverting a commit"],
   "queries": ["my rebase hit a conflict", "stuck in the middle of a rebase", "how do i finish an interactive rebase",
               "rebase stopped and i dont know what to do", "resolve conflicts while rebasing",
               "i lost commits during a rebase", "continue after fixing rebase conflicts", "abort a bad rebase"]},

  {"name": "git-recover-deleted-branch",
   "description": "Recover commits from a branch that was deleted or reset away, using the reflog.",
   "applies_when": ["deleted branch", "lost commits", "git reflog", "hard reset recovery"],
   "symptoms": ["branch not found", "commits gone after reset --hard"],
   "not_for": ["rebase conflicts", "untracked files never committed", "stash"],
   "queries": ["i deleted my branch by accident", "get my commits back after a hard reset",
               "recover a branch i just removed", "my work disappeared after git reset",
               "how do i undo a branch deletion", "find lost commits in git", "restore deleted work in git",
               "i think i lost a days work in git"]},

  {"name": "docker-layer-cache-miss",
   "description": "Fix a Dockerfile that rebuilds everything on every change because the layer cache is invalidated too early.",
   "applies_when": ["docker build slow", "layer cache invalidated", "dockerfile ordering"],
   "symptoms": ["every build reinstalls dependencies", "no cached layers used"],
   "not_for": ["container wont start", "image size", "docker compose networking"],
   "queries": ["my docker build is really slow", "docker reinstalls deps every time",
               "why is nothing cached in my docker build", "speed up docker builds",
               "dockerfile rebuilds everything on a code change", "docker cache never hits",
               "make my image build faster", "docker build takes ten minutes"]},

  {"name": "docker-container-exits-immediately",
   "description": "Diagnose a container that starts and exits straight away with no useful output.",
   "applies_when": ["container exits", "container wont stay up", "exited (0)", "crashloop"],
   "symptoms": ["Exited (0) 2 seconds ago", "container keeps restarting"],
   "not_for": ["build failures", "slow builds", "registry auth"],
   "queries": ["my container exits right away", "container starts then stops",
               "docker run does nothing and exits", "why wont my container stay running",
               "container keeps restarting in a loop", "the container dies instantly",
               "no logs and the container is gone", "docker container exited 0"]},

  {"name": "pytest-flaky-order-dependence",
   "description": "Fix tests that pass alone but fail when the whole suite runs, because of shared state between tests.",
   "applies_when": ["test passes alone fails in suite", "order dependent test", "shared state between tests"],
   "symptoms": ["passes with -k but fails in full run", "fails only on rerun"],
   "not_for": ["collection errors", "import errors", "slow tests"],
   "queries": ["test passes on its own but fails with the others", "my test suite is flaky",
               "tests fail only when run together", "why does the order of my tests matter",
               "test works alone breaks in ci", "random test failures in the suite",
               "one test is polluting the others", "suite fails but individual tests pass"]},

  {"name": "pytest-collection-error",
   "description": "Fix pytest failing at collection time so that no tests run at all.",
   "applies_when": ["pytest collection error", "no tests ran", "conftest import failure"],
   "symptoms": ["ERROR collecting", "no tests ran in", "ImportError while loading conftest"],
   "not_for": ["failing assertions", "flaky tests", "coverage config"],
   "queries": ["pytest wont collect my tests", "no tests ran at all",
               "error collecting test file", "pytest fails before running anything",
               "conftest is breaking my test run", "pytest cant import my test module",
               "zero tests collected", "my tests arent being discovered"]},

  {"name": "postgres-missing-index-slow-query",
   "description": "Speed up a slow Postgres query that is doing a sequential scan for want of an index.",
   "applies_when": ["slow postgres query", "sequential scan", "missing index", "explain analyze"],
   "symptoms": ["Seq Scan on", "query takes seconds"],
   "not_for": ["connection pool exhaustion", "migrations", "deadlocks"],
   "queries": ["this query is really slow", "postgres is doing a seq scan",
               "how do i speed up this select", "my database query takes forever",
               "explain analyze shows a sequential scan", "should i add an index here",
               "slow query on a big table", "postgres performance is bad on this query"]},

  {"name": "postgres-connection-pool-exhausted",
   "description": "Fix an application that intermittently cannot get a Postgres connection because the pool is exhausted.",
   "applies_when": ["connection pool exhausted", "too many clients", "leaked connection"],
   "symptoms": ["FATAL: sorry, too many clients already", "pool timeout waiting for connection"],
   "not_for": ["slow queries", "missing indexes", "schema migrations"],
   "queries": ["too many clients already error", "my app cant get a database connection",
               "connection pool keeps running out", "postgres refuses new connections under load",
               "we leak database connections", "intermittent db connection timeouts",
               "app hangs waiting for a connection", "ran out of postgres connections"]},

  {"name": "bash-unicode-case-conversion",
   "description": "Convert text case correctly when the input may contain non-ASCII characters.",
   "applies_when": ["uppercase a file", "lowercase text", "case conversion", "accented characters"],
   "symptoms": ["accented characters unchanged", "tr left non-ascii alone"],
   "not_for": ["case-insensitive search", "encoding conversion", "sorting"],
   "queries": ["make this file uppercase", "convert the text to lowercase",
               "uppercase everything in the file", "change the case of this text",
               "my accented letters didnt get uppercased", "turn these names into caps",
               "lowercase a text file", "capitalise the whole file"]},

  {"name": "bash-exit-code-hides-failure",
   "description": "Verify a script actually had its intended effect when the script exits 0 regardless of what happened.",
   "applies_when": ["exit 0 but nothing happened", "script lies about success", "verify side effect"],
   "symptoms": ["exit 0 but no output file", "command succeeded but did nothing"],
   "not_for": ["syntax errors", "permission denied", "case conversion"],
   "queries": ["the script says it worked but nothing changed", "exit code 0 but no output",
               "how do i know the script actually did anything", "command succeeds but produces no file",
               "my build script always returns success", "it reports success but the file isnt there",
               "verify the script really ran", "script exits 0 even when it fails"]}
]
